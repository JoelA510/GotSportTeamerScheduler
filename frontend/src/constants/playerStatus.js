/**
 * Badge tone per player status (players.status CHECK values).
 * Shared by the roster grid and the player record page.
 */
export const STATUS_TONE = {
  active: 'success',
  pending: 'neutral',
  inactive: 'neutral',
  waitlist: 'warning',
};
